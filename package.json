{
  "name": "argus-dashboard",
  "version": "0.0.1",
  "private": true,
  "description": "Argus Hermes dashboard plugin — React frontend, esbuild IIFE bundle.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
