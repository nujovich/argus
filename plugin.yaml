name: argus
version: "0.0.1"
description: "Argus — horizontal financial control plane for money-spending agents. Meters every dollar, tracks live P&L per job, and gates Stripe spends through a HITL approval flow. Scaffold only — no hooks registered yet."
author: nujovich
# Hooks will be registered in __init__.py once Phase 3 implements Capture +
# Enforcement. Declared here for documentation; see CLAUDE.md §6.
hooks: []
